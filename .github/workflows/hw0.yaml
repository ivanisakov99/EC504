name: HW0 - Integer Power

on:
  push:
    branches: [dev]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build & run programme
        run: make -C hw0 all
      - name: Run programme
        run: cd hw0 && ./power
